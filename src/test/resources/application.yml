spring:
  datasource:
    url: jdbc:h2:mem:jpa-shop
#    url: jdbc:h2:tcp://localhost/~/jpa-shop;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
#        show-sql: true
        format-sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type: trace

